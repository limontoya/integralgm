<h1>Editing Contacto</h1>

<%= render 'form', contacto: @contacto %>

  <% if !current_user.nil? %> 
    <% if current_user.user_role? %>
      <%= link_to 'Show', @contacto %> |
	  <%= link_to 'Back', contactos_path %>
    <% end %>
  <% end %>

