
<br><h2 class="text-primary">Editar Producto</h2><br>

<%= render 'form', producto: @producto %>

<% if !current_user.nil? %> 
	<% if current_user.user_role? %>
  		<%= link_to 'Show', @producto %> |
		<%= link_to 'Back', productos_path %>
	<% end %>
<% end %>


